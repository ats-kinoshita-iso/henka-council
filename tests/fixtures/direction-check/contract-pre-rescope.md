# Sprint 09 Contract (PRE-RESCOPE)

## What I Will Build

A `.cmpx` writer (`serialization.write_cmpx`) plus ranked-node TNormal subset
support and round-trip identity tests.

## Success Criteria

1. Round-trip a parsed model back to `.cmpx` with CPT identity at 1e-10.

(No citation of the strategic anchor; no ADR reference.)
