# Violation: missing-required-field

## Rule violated
`resolution_status` is a required property per the schema. This fixture omits it.

## Expected jsonschema error keyword
`required`

## Spec section
§11.8 conflict-resolution-entry schema — `required` array includes `resolution_status`.
