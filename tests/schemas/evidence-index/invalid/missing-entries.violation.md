# Violation: missing-entries

## Rule violated
`entries` is a required property per the schema. This fixture omits it entirely.

## Expected jsonschema error keyword
`required`

## Spec section
§11.9 evidence-index schema — `required` array includes `entries`.
