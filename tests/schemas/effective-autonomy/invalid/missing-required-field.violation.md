# Violation: missing-required-field

## Rule violated
`reason` is a required property. This fixture omits it.

## Expected jsonschema error keyword
`required`

## Spec section
§11.11 effective-autonomy schema (NEW v2/R10) — `required` array includes `reason`.
