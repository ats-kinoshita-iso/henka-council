# Violation: procedures array is empty (minItems: 1)

## Rule violated
`procedures` must contain at least 1 item (`minItems: 1`). An empty array `[]` violates this constraint.

## Expected jsonschema error keyword
`minItems`

## Spec section
§11.5 — The `procedures` array requires at least one procedure entry defining council governance steps.
